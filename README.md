# 📝 Fullstack TODO App


Простое и функциональное приложение для управления списками дел с разделением по владельцам. Проект включает в себя серверную часть на чистом **Node.js** и клиентскую часть на **Vanilla JS**.

---

## 🚀 Особенности

*   **Native Backend**: Сервер написан без использования фреймворков (Express и др.), только встроенные модули Node.js.
*   **Persistent Storage**: Данные сохраняются в локальный JSON-файл (`db.json`) и не пропадают после перезагрузки сервера.
*   **Multi-Page**: Разные списки дел для разных пользователей (Мама, Папа, Я) с фильтрацией на стороне сервера.
*   **REST API**: Полный цикл CRUD операций (создание, чтение, обновление, удаление).

---

## 🛠 Технологический стек

*   **Frontend**: HTML5, CSS3 (Bootstrap 4), JavaScript (Fetch API).
*   **Backend**: Node.js (модули `http`, `fs`).
*   **База данных**: JSON файл.

---

## 📋 Схема данных (Todo Item)

Каждый объект задачи в базе данных имеет следующую структуру:

| Поле    | Тип      | Описание                                     |
| :------ | :------- | :------------------------------------------- |
| `id`    | `string` | Уникальный идентификатор (генерируется сервером) |
| `name`  | `string` | Название/заголовок задачи (обязательно)      |
| `owner` | `string` | Владелец задачи (фильтрация по `?owner=`)    |
| `done`  | `boolean`| Статус выполнения (по умолчанию `false`)     |

---

## 📦 Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Rrom-Ka/todo-server.git
   cd todo-server
